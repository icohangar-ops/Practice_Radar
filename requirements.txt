# Practice Radar – Python dependencies
# Install with: pip install -r requirements.txt
# Requires Python 3.12+ (numpy==2.5.2 needs Python >= 3.12)

pandas>=2.0.0
numpy==2.5.2
openpyxl>=3.1.0

requests==2.34.0